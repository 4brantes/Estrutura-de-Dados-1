#include <stdio.h>
#include<stdlib.h> 
#define QTD_Alunos 2 //constante
#include "aluno.c"

int main(void){
    Aluno* aluno = (Aluno*)malloc(sizeof(aluno));
    if (aluno == NULL){
        exit(1);
    }
    preenche(aluno);
    imprime(aluno);
    return 0;
}
